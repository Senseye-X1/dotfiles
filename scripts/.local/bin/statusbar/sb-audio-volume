#!/bin/sh

case $BLOCK_BUTTON in
	1) pamixer -t ;;
	2) pamixer -t ;;
	4) pamixer -i 5 ;;
	5) pamixer -d 5 ;;
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

[ $(pamixer --get-mute) = true ] && echo "MUTE" && exit

vol="$(pamixer --get-volume)"

#echo "$vol%"
printf "%03d%%" "$vol"
